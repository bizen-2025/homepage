<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회사소개 히어로 섹션</title>
    <style>
        @import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif;
            background: transparent;
        }

        /* 히어로 타이틀 섹션 */
        #s20251014b0c68fbc1c20e {
            position: relative;
            width: 100%;
            min-height: 60vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 120px 20px 80px;
            background: transparent;
        }

        /* 타이틀 컨테이너 */
        .hero-container {
            position: relative;
            z-index: 2;
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
            animation: fadeInUp 1s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* 배지 */
        #s20251014b0c68fbc1c20e .hero-badge {
            display: inline-block;
            padding: 10px 30px;
            background: rgba(0, 255, 255, 0.1);
            border: 1px solid rgba(0, 255, 255, 0.3);
            border-radius: 50px;
            color: #00ffff;
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 30px;
            letter-spacing: 2px;
            text-transform: uppercase;
            backdrop-filter: blur(10px);
            animation: fadeIn 1s ease 0.2s backwards;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* 메인 타이틀 */
        #s20251014b0c68fbc1c20e .hero-title {
            font-size: clamp(42px, 6vw, 72px);
            font-weight: 900;
            color: #ffffff;
            margin-bottom: 30px;
            line-height: 1.2;
            letter-spacing: -1px;
            animation: fadeInUp 1s ease 0.4s backwards;
        }

        #s20251014b0c68fbc1c20e .hero-title .highlight {
            color: #00ffff;
            text-shadow:
                0 0 30px rgba(0, 255, 255, 0.8),
                0 0 60px rgba(0, 255, 255, 0.4);
            display: inline-block;
            animation: pulse 2s ease infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.8; }
        }

        /* 서브타이틀 */
        #s20251014b0c68fbc1c20e .hero-subtitle {
            font-size: clamp(20px, 3vw, 28px);
            color: rgba(255, 255, 255, 0.95);
            line-height: 1.6;
            max-width: 800px;
            margin: 0 auto 40px;
            font-weight: 400;
            animation: fadeInUp 1s ease 0.6s backwards;
        }

        /* 통계 카운터 섹션 */
        #s20251014b0c68fbc1c20e .stats-container {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 50px;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease 0.8s backwards;
        }

        #s20251014b0c68fbc1c20e .stat-item {
            text-align: center;
            padding: 20px;
            min-width: 150px;
        }

        #s20251014b0c68fbc1c20e .stat-number {
            font-size: 42px;
            font-weight: 900;
            color: #00ffff;
            text-shadow: 0 0 20px rgba(0, 255, 255, 0.6);
            margin-bottom: 8px;
            display: block;
        }

        #s20251014b0c68fbc1c20e .counter {
            display: inline-block;
            min-width: 1.2em;
        }

        #s20251014b0c68fbc1c20e .stat-label {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* 스크롤 인디케이터 */
        #s20251014b0c68fbc1c20e .scroll-indicator {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateX(-50%) translateY(0); }
            50% { transform: translateX(-50%) translateY(-10px); }
        }

        #s20251014b0c68fbc1c20e .scroll-indicator svg {
            width: 30px;
            height: 30px;
            fill: rgba(255, 255, 255, 0.5);
        }

        /* 반응형 - 태블릿 */
        @media (min-width: 768px) {
            #s20251014b0c68fbc1c20e {
                min-height: 70vh;
                padding: 140px 40px 100px;
            }

            #s20251014b0c68fbc1c20e .stats-container {
                gap: 60px;
            }

            #s20251014b0c68fbc1c20e .stat-item {
                min-width: 180px;
            }

            #s20251014b0c68fbc1c20e .stat-number {
                font-size: 48px;
            }

            #s20251014b0c68fbc1c20e .stat-label {
                font-size: 16px;
            }
        }

        /* 반응형 - 모바일 */
        @media (max-width: 767px) {
            #s20251014b0c68fbc1c20e {
                min-height: 50vh;
                padding: 100px 16px 60px;
            }

            #s20251014b0c68fbc1c20e .hero-title {
                font-size: 36px;
            }

            #s20251014b0c68fbc1c20e .hero-subtitle {
                font-size: 18px;
                margin-bottom: 30px;
            }

            #s20251014b0c68fbc1c20e .hero-badge {
                font-size: 12px;
                padding: 8px 20px;
            }

            #s20251014b0c68fbc1c20e .stats-container {
                gap: 20px;
                margin-top: 30px;
            }

            #s20251014b0c68fbc1c20e .stat-item {
                min-width: 120px;
                padding: 15px;
            }

            #s20251014b0c68fbc1c20e .stat-number {
                font-size: 36px;
            }

            #s20251014b0c68fbc1c20e .stat-label {
                font-size: 12px;
            }
        }
    </style>
</head>
<body>
    <!-- 히어로 타이틀 섹션 -->
    <section id="s20251014b0c68fbc1c20e">
        <!-- 타이틀 컨텐츠 -->
        <div class="hero-container">
            <span class="hero-badge">About MJGOOD</span>
            <h1 class="hero-title">
                정책자금 성공의<br>
                <span class="highlight">전문 파트너</span>
            </h1>
            <p class="hero-subtitle">
                MJGOOD은 체계적인 분석과 맞춤형 전략으로<br>
                귀사의 성공적인 자금 확보를 지원합니다
            </p>

            <!-- 통계 카운터 -->
            <div class="stats-container">
                <div class="stat-item">
                    <span class="stat-number">
                        <span class="counter" data-target="92">0</span>%
                    </span>
                    <span class="stat-label">승인률</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">
                        <span class="counter" data-target="3.2" data-decimal="1">0.0</span>억
                    </span>
                    <span class="stat-label">평균 지원금</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">
                        <span class="counter" data-target="150">0</span>+
                    </span>
                    <span class="stat-label">성공 사례</span>
                </div>
            </div>
        </div>

        <!-- 스크롤 인디케이터 -->
        <div class="scroll-indicator">
            <svg viewBox="0 0 24 24">
                <path d="M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z"/>
            </svg>
        </div>
    </section>

    <script>
        // 카운팅 애니메이션
        document.addEventListener('DOMContentLoaded', function() {
            const counters = document.querySelectorAll('.counter');
            const speed = 2000; // 애니메이션 시간 (ms)

            const startCounting = (counter) => {
                const target = parseFloat(counter.getAttribute('data-target'));
                const hasDecimal = counter.getAttribute('data-decimal');
                const increment = target / (speed / 16); // 60fps 기준
                let current = 0;

                const updateCounter = () => {
                    current += increment;

                    if (current < target) {
                        if (hasDecimal) {
                            counter.textContent = current.toFixed(1);
                        } else {
                            counter.textContent = Math.floor(current);
                        }
                        requestAnimationFrame(updateCounter);
                    } else {
                        if (hasDecimal) {
                            counter.textContent = target.toFixed(1);
                        } else {
                            counter.textContent = target;
                        }
                    }
                };

                updateCounter();
            };

            // Intersection Observer로 화면에 보일 때 카운팅 시작
            const observerOptions = {
                threshold: 0.5,
                rootMargin: '0px'
            };

            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const counter = entry.target;
                        if (counter.textContent === '0' || counter.textContent === '0.0') {
                            startCounting(counter);
                        }
                    }
                });
            }, observerOptions);

            counters.forEach(counter => {
                observer.observe(counter);
            });
        });
    </script>
</body>
</html>