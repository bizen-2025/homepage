# 아임웹 → Next.js 마이그레이션 가이드

**작성일**: 2025-11-24
**목표**: 기존 아임웹 구조를 Next.js로 1:1 전환

---

## 📋 목차

1. [마이그레이션 전략](#1-마이그레이션-전략)
2. [페이지 구조 매핑](#2-페이지-구조-매핑)
3. [섹션별 컴포넌트 전환](#3-섹션별-컴포넌트-전환)
4. [브랜드 컬러 변경](#4-브랜드-컬러-변경)
5. [단계별 작업 순서](#5-단계별-작업-순서)

---

## 1. 마이그레이션 전략

### 1.1 기본 원칙

```
✅ 유지: 메뉴 구조, 섹션 순서, 콘텐츠 내용
✅ 변경: 브랜드 컬러만
✅ 추가: SEO 최적화 (메타 태그, Structured Data)
✅ 기술: HTML 위젯 → Next.js 컴포넌트
```

### 1.2 작업 방식

```
기존 아임웹 파일 (F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터)
    ↓
HTML/CSS 분석
    ↓
Next.js 컴포넌트로 변환 (Tailwind CSS)
    ↓
브랜드 컬러 적용
    ↓
SEO 메타 태그 추가
```

---

## 2. 페이지 구조 매핑

### 2.1 아임웹 → Next.js 경로 매핑

| 아임웹 페이지 | Next.js 경로 | 파일 위치 |
|-------------|-------------|----------|
| 홈 | `/` | `app/page.tsx` |
| 회사소개 | `/company` | `app/company/page.tsx` |
| 진행과정 | `/process` | `app/process/page.tsx` |
| 자금상담 | `/fund` | `app/fund/page.tsx` |
| 전문서비스 | `/service` | `app/service/page.tsx` |
| 성공사례 | `/success` | `app/success/page.tsx` |
| FAQ | `/faq` | `app/faq/page.tsx` |
| 상담신청 | `/contact` | `app/contact/page.tsx` |

**변경 사항**: 없음 (동일한 URL 구조)

---

## 3. 섹션별 컴포넌트 전환

### 3.1 전환 프로세스

#### Step 1: 기존 HTML 파일 열기
```bash
# 예: 히어로 섹션
F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터\02.hero.txt
```

#### Step 2: HTML 구조 분석
```html
<!-- 기존 아임웹 HTML 예시 -->
<section id="hero" style="background: #0066CC; padding: 100px 0;">
  <div class="container">
    <h1>정책자금 전문 컨설팅</h1>
    <p>중소기업·소상공인 맞춤 지원</p>
    <a href="#contact" class="cta-button">무료 상담 신청</a>
  </div>
</section>
```

#### Step 3: Next.js 컴포넌트로 변환
```tsx
// components/home/HeroSection.tsx
export function HeroSection() {
  return (
    <section className="bg-primary py-24 md:py-32">
      <div className="container mx-auto px-4">
        <h1 className="text-5xl font-bold text-white mb-4">
          정책자금 전문 컨설팅
        </h1>
        <p className="text-xl text-white/90 mb-8">
          중소기업·소상공인 맞춤 지원
        </p>
        <Link
          href="/contact"
          className="inline-block bg-accent text-white px-8 py-4 rounded-lg hover:bg-accent/90"
        >
          무료 상담 신청
        </Link>
      </div>
    </section>
  );
}
```

### 3.2 CSS 변환 규칙

#### 인라인 스타일 → Tailwind CSS

| 기존 CSS | Tailwind Class |
|---------|---------------|
| `background: #0066CC` | `bg-primary` |
| `padding: 100px 0` | `py-24` (모바일) / `md:py-32` (데스크톱) |
| `font-size: 42px` | `text-5xl` |
| `font-weight: 700` | `font-bold` |
| `color: #FFFFFF` | `text-white` |
| `margin-bottom: 16px` | `mb-4` |
| `border-radius: 8px` | `rounded-lg` |
| `display: flex` | `flex` |
| `align-items: center` | `items-center` |
| `justify-content: space-between` | `justify-between` |

#### 반응형 변환

```css
/* 기존 (미디어 쿼리) */
.hero-title {
  font-size: 32px;
}

@media (min-width: 768px) {
  .hero-title {
    font-size: 48px;
  }
}
```

```tsx
// 변환 후 (Tailwind)
<h1 className="text-3xl md:text-5xl">정책자금 전문 컨설팅</h1>
```

---

## 4. 브랜드 컬러 변경

### 4.1 색상 정의

#### 기존 아임웹 색상 추출
```css
/* 기존 아임웹에서 사용 중인 색상 */
/* F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터 파일들에서 추출 */

주요 색상:
- Primary: #XXXXXX (기존 색상)
- Secondary: #XXXXXX
- Accent: #XXXXXX
```

#### 신규 브랜드 컬러 적용
```typescript
// tailwind.config.ts
export default {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#0066CC',      // 메인 브랜드 컬러
          dark: '#003D7A',         // 어두운 버전
          light: '#E6F2FF',        // 밝은 버전
        },
        secondary: '#6B7280',      // 보조 컬러
        accent: '#F59E0B',         // 강조 컬러 (CTA 버튼)
        success: '#10B981',
        warning: '#F59E0B',
        error: '#EF4444',
      },
    },
  },
};
```

### 4.2 전역 CSS 변수

```css
/* app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    /* 브랜드 컬러 */
    --color-primary: #0066CC;
    --color-primary-dark: #003D7A;
    --color-primary-light: #E6F2FF;
    --color-accent: #F59E0B;

    /* 폰트 */
    --font-pretendard: 'Pretendard', -apple-system, sans-serif;
  }
}
```

### 4.3 색상 일괄 변경 스크립트

```bash
# 모든 컴포넌트에서 색상 일괄 변경
# 예: 기존 파란색(#0066CC)을 새 브랜드 컬러로 변경

# Linux/Mac
find components -type f -name "*.tsx" -exec sed -i 's/#0066CC/bg-primary/g' {} +

# Windows (PowerShell)
Get-ChildItem -Path components -Recurse -Filter *.tsx |
  ForEach-Object { (Get-Content $_.FullName) -replace '#0066CC', 'bg-primary' |
  Set-Content $_.FullName }
```

---

## 5. 단계별 작업 순서

### Phase 1: 프로젝트 설정 (Day 1)

```bash
# 1. Next.js 프로젝트 생성
npx create-next-app@latest pola-homepage --typescript --tailwind --app

# 2. 디렉토리 이동
cd pola-homepage

# 3. shadcn/ui 초기화
npx shadcn@latest init

# 4. 필수 컴포넌트 설치
npx shadcn@latest add button input card accordion dialog toast

# 5. 추가 패키지
npm install airtable resend react-hook-form zod @hookform/resolvers
```

### Phase 2: 레이아웃 컴포넌트 (Day 2)

**작업 순서**:
1. ✅ Header 컴포넌트 생성
2. ✅ Footer 컴포넌트 생성
3. ✅ MobileMenu 컴포넌트 생성

**파일**:
```
components/layout/
├── Header.tsx
├── Footer.tsx
└── MobileMenu.tsx
```

**참고 파일**:
```
F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터\01.header.txt
F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터\XX.footer.txt (있으면)
```

### Phase 3: 홈페이지 섹션 (Day 3-5)

**작업 순서**:
1. ✅ HeroSection (히어로)
2. ✅ ServiceSection (서비스 소개)
3. ✅ ProcessSection (진행 과정)
4. ✅ StatsSection (통계 - 선택)
5. ✅ FAQSection (FAQ 미리보기)
6. ✅ CTASection (상담 신청 CTA)

**파일**:
```
components/home/
├── HeroSection.tsx
├── ServiceSection.tsx
├── ProcessSection.tsx
├── StatsSection.tsx
├── FAQSection.tsx
└── CTASection.tsx
```

**참고 파일**:
```
F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터\
├── 02.hero.txt
├── 03.service.txt
├── 04.process.txt
└── ... (기타 섹션)
```

### Phase 4: 서브 페이지 (Day 6-8)

**작업 순서**:
1. ✅ 회사소개 (`/company`)
2. ✅ 진행과정 (`/process`)
3. ✅ 자금상담 (`/fund`)
4. ✅ 전문서비스 (`/service`)
5. ✅ 성공사례 (`/success`) - 목록만
6. ✅ FAQ (`/faq`)

**파일**:
```
app/
├── company/page.tsx
├── process/page.tsx
├── fund/page.tsx
├── service/page.tsx
├── success/page.tsx
└── faq/page.tsx
```

### Phase 5: 폼 & API (Day 9-10)

**작업 순서**:
1. ✅ ContactForm 컴포넌트
2. ✅ API Route (`/api/submit-form`)
3. ✅ Airtable 연동
4. ✅ Resend 이메일
5. ✅ Telegram 알림

**파일**:
```
components/forms/
└── ContactForm.tsx

app/api/
└── submit-form/
    └── route.ts

lib/
├── airtable.ts
├── email.ts
└── telegram.ts
```

### Phase 6: SEO 최적화 (Day 11-12)

**작업 순서**:
1. ✅ 모든 페이지 메타 태그 추가
2. ✅ Sitemap 생성
3. ✅ Robots.txt 설정
4. ✅ Structured Data (JSON-LD)
5. ✅ Open Graph 이미지

**파일**:
```
app/
├── sitemap.ts
├── robots.ts
└── og/route.tsx

components/seo/
└── StructuredData.tsx
```

### Phase 7: 테스트 & 배포 (Day 13-14)

**체크리스트**:
- [ ] 모든 페이지 동작 확인
- [ ] 폼 제출 테스트
- [ ] 반응형 디자인 확인 (모바일, 태블릿, 데스크톱)
- [ ] 브라우저 호환성 테스트
- [ ] Lighthouse 점수 확인 (90점 이상)
- [ ] 환경변수 프로덕션 설정
- [ ] Vercel 배포

---

## 6. 컴포넌트 변환 템플릿

### 6.1 섹션 컴포넌트 기본 구조

```tsx
// components/[폴더]/[이름]Section.tsx

interface [이름]SectionProps {
  // props가 필요한 경우 정의
}

export function [이름]Section(props: [이름]SectionProps) {
  return (
    <section className="py-16 md:py-24 bg-[배경색]">
      <div className="container mx-auto px-4">
        {/* 섹션 내용 */}
        <div className="text-center mb-12">
          <h2 className="text-3xl md:text-4xl font-bold mb-4">
            섹션 제목
          </h2>
          <p className="text-lg text-gray-600">
            섹션 설명
          </p>
        </div>

        {/* 메인 콘텐츠 */}
        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {/* 그리드 아이템 */}
        </div>
      </div>
    </section>
  );
}
```

### 6.2 페이지 컴포넌트 기본 구조

```tsx
// app/[경로]/page.tsx

import { Metadata } from 'next';

export const metadata: Metadata = {
  title: '페이지 제목 | POLA',
  description: '페이지 설명 (120~160자)',
  alternates: { canonical: 'https://pola.com/경로' },
};

export default function [페이지명]Page() {
  return (
    <>
      {/* 섹션 1 */}
      <[섹션1]Section />

      {/* 섹션 2 */}
      <[섹션2]Section />

      {/* 섹션 3 */}
      <[섹션3]Section />
    </>
  );
}
```

---

## 7. 이미지 마이그레이션

### 7.1 이미지 파일 구조

```
public/images/
├── hero/
│   ├── main.jpg              # 히어로 메인 이미지
│   └── main.webp             # WebP 버전
├── services/
│   ├── consulting.jpg
│   ├── planning.jpg
│   └── analysis.jpg
├── cases/
│   └── success-*.jpg         # 성공사례 이미지
└── icons/
    └── *.svg                 # 아이콘
```

### 7.2 이미지 복사

```bash
# 기존 아임웹 이미지 → public/images로 복사
cp F:\bas_homepage\25.yangmihyeon_yj정책자금지원센터\images/* F:\pola_homepage\public\images\
```

### 7.3 Next.js Image 컴포넌트 사용

```tsx
import Image from 'next/image';

// ✅ 올바른 방법
<Image
  src="/images/hero/main.jpg"
  alt="정책자금 컨설팅"
  width={1200}
  height={600}
  priority
  className="object-cover"
/>

// ❌ 잘못된 방법
<img src="/images/hero/main.jpg" alt="..." />
```

---

## 8. 환경변수 설정

### .env.local
```bash
# Airtable
AIRTABLE_TOKEN=patXXXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXXXX

# Resend (Email)
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXX

# Telegram
TELEGRAM_BOT_TOKEN=XXXXXXXXX:XXXXXXXXXXXXXXXXXXXXXXXXXXX
TELEGRAM_CHAT_ID=-XXXXXXXXXXXXX

# Site
NEXT_PUBLIC_SITE_URL=https://pola.com
```

---

## 9. 배포 체크리스트

### 배포 전
- [ ] 모든 환경변수 Vercel에 설정
- [ ] 이미지 최적화 (WebP 변환)
- [ ] Lighthouse 점수 90점 이상
- [ ] 크로스 브라우저 테스트 (Chrome, Safari, Firefox)
- [ ] 모바일 반응형 확인
- [ ] 폼 제출 테스트 (실제 Airtable 연동)
- [ ] 이메일 발송 테스트
- [ ] Google Search Console 준비

### 배포 후
- [ ] Google Search Console 등록
- [ ] Sitemap 제출
- [ ] Google Analytics 설치 (선택)
- [ ] 도메인 연결
- [ ] SSL 인증서 확인

---

## 10. 트러블슈팅

### 자주 발생하는 문제

#### 1. Tailwind CSS 클래스 적용 안 됨
```bash
# 해결: Tailwind 설정 확인
# tailwind.config.ts
content: [
  './app/**/*.{ts,tsx}',
  './components/**/*.{ts,tsx}',
]
```

#### 2. 이미지 로딩 느림
```tsx
// 해결: priority 속성 추가
<Image src="..." alt="..." priority />
```

#### 3. 폼 제출 실패
```typescript
// 해결: CORS 설정 확인
// next.config.js
async headers() {
  return [
    {
      source: '/api/:path*',
      headers: [
        { key: 'Access-Control-Allow-Origin', value: '*' },
      ],
    },
  ];
}
```

---

## 11. 완료 기준

### 각 단계별 완료 기준

#### Phase 1: 프로젝트 설정
- [x] `npm run dev` 정상 실행
- [x] Tailwind CSS 작동 확인
- [x] shadcn/ui 컴포넌트 임포트 가능

#### Phase 2: 레이아웃
- [x] Header 모든 메뉴 표시
- [x] Footer 모든 정보 표시
- [x] 모바일 메뉴 정상 작동

#### Phase 3: 홈페이지
- [x] 모든 섹션 표시
- [x] 반응형 디자인 적용
- [x] CTA 버튼 클릭 시 `/contact`로 이동

#### Phase 4: 서브 페이지
- [x] 각 페이지 접근 가능
- [x] 메타 태그 설정 완료
- [x] 브랜드 컬러 적용 확인

#### Phase 5: 폼 & API
- [x] 폼 제출 성공
- [x] Airtable에 데이터 저장 확인
- [x] 이메일 수신 확인
- [x] Telegram 알림 수신

#### Phase 6: SEO
- [x] Google Search Console에서 Sitemap 인식
- [x] 모든 페이지 메타 태그 확인
- [x] Structured Data 검증 통과

#### Phase 7: 배포
- [x] Vercel 배포 성공
- [x] 도메인 연결 (선택)
- [x] HTTPS 적용
- [x] 모든 기능 프로덕션 환경에서 작동

---

**문서 버전**: 1.0.0
**작성일**: 2025-11-24
**작성자**: Claude (AI Assistant)
