# POLA 홈페이지 프로젝트 구조 기획서

**작성일**: 2025-11-24
**프로젝트명**: POLA 정책자금 컨설팅 홈페이지
**목표**: 체계적인 폴더 구조 및 파일 네이밍으로 유지보수성 극대화

---

## 📋 목차

1. [전체 프로젝트 구조](#1-전체-프로젝트-구조)
2. [app/ 디렉토리 상세](#2-app-디렉토리-상세)
3. [components/ 디렉토리 상세](#3-components-디렉토리-상세)
4. [lib/ 디렉토리 상세](#4-lib-디렉토리-상세)
5. [data/ 디렉토리 상세](#5-data-디렉토리-상세)
6. [public/ 디렉토리 상세](#6-public-디렉토리-상세)
7. [설정 파일](#7-설정-파일)
8. [네이밍 컨벤션](#8-네이밍-컨벤션)
9. [Import 경로 규칙](#9-import-경로-규칙)

---

## 1. 전체 프로젝트 구조

```
pola-homepage/
├── app/                          # Next.js 15 App Router
│   ├── (marketing)/              # 마케팅 페이지 그룹
│   ├── (legal)/                  # 법적 페이지 그룹
│   ├── api/                      # API Routes
│   ├── layout.tsx                # 루트 레이아웃
│   ├── page.tsx                  # 홈페이지
│   ├── sitemap.ts                # Sitemap 생성
│   ├── robots.ts                 # Robots.txt
│   └── globals.css               # 전역 CSS
│
├── components/                   # React 컴포넌트
│   ├── layout/                   # 레이아웃 컴포넌트
│   ├── home/                     # 홈페이지 섹션
│   ├── success/                  # 성공사례 컴포넌트
│   ├── blog/                     # 블로그 컴포넌트
│   ├── faq/                      # FAQ 컴포넌트
│   ├── forms/                    # 폼 컴포넌트
│   ├── seo/                      # SEO 컴포넌트
│   └── ui/                       # shadcn/ui 컴포넌트
│
├── lib/                          # 유틸리티 및 헬퍼
│   ├── airtable.ts               # Airtable SDK
│   ├── email.ts                  # Resend SDK
│   ├── telegram.ts               # Telegram API
│   ├── seo-analytics.ts          # SEO 분석
│   ├── keywords.ts               # 키워드 관리
│   ├── utils.ts                  # 공통 유틸
│   └── constants.ts              # 상수 정의
│
├── data/                         # 정적 데이터
│   ├── success-cases.json        # 성공사례
│   ├── faq-data.json             # FAQ
│   └── blog-posts/               # 블로그 MDX
│
├── public/                       # 정적 파일
│   ├── images/                   # 이미지
│   ├── fonts/                    # 폰트
│   └── favicon.ico               # 파비콘
│
├── types/                        # TypeScript 타입
│   ├── index.ts                  # 공통 타입
│   ├── airtable.ts               # Airtable 타입
│   └── seo.ts                    # SEO 타입
│
├── hooks/                        # Custom React Hooks
│   ├── use-form-submission.ts   # 폼 제출 훅
│   └── use-scroll-spy.ts        # 스크롤 스파이
│
├── styles/                       # CSS/SCSS
│   └── tailwind.css              # Tailwind 커스텀
│
├── scripts/                      # 빌드 스크립트
│   └── backup-airtable.ts       # Airtable 백업
│
├── .env.local                    # 환경변수 (Git 제외)
├── .env.example                  # 환경변수 템플릿
├── .gitignore                    # Git 제외 파일
├── next.config.js                # Next.js 설정
├── tailwind.config.ts            # Tailwind 설정
├── tsconfig.json                 # TypeScript 설정
├── package.json                  # 패키지 의존성
└── README.md                     # 프로젝트 문서
```

---

## 2. app/ 디렉토리 상세

### 2.1 루트 파일

```
app/
├── layout.tsx                    # 전역 레이아웃 (헤더, 푸터, SEO)
├── page.tsx                      # 홈페이지 (/)
├── sitemap.ts                    # 동적 Sitemap 생성
├── robots.ts                     # Robots.txt 생성
├── not-found.tsx                 # 404 페이지
├── error.tsx                     # 에러 페이지
├── loading.tsx                   # 로딩 상태
└── globals.css                   # 전역 CSS (Tailwind)
```

**파일별 역할**:

#### layout.tsx
```tsx
// app/layout.tsx
import { Metadata } from 'next';
import { Header } from '@/components/layout/Header';
import { Footer } from '@/components/layout/Footer';
import { Analytics } from '@vercel/analytics/react';
import { StructuredData } from '@/components/seo/StructuredData';

export const metadata: Metadata = {
  title: {
    default: 'POLA - 정책자금 컨설팅',
    template: '%s | POLA',
  },
  description: '중소기업·소상공인 정책자금 전문 컨설팅',
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body className="font-pretendard">
        <StructuredData type="organization" />
        <Header />
        <main>{children}</main>
        <Footer />
        <Analytics />
      </body>
    </html>
  );
}
```

#### page.tsx (홈페이지)
```tsx
// app/page.tsx
import { Metadata } from 'next';
import { HeroSection } from '@/components/home/HeroSection';
import { ServiceSection } from '@/components/home/ServiceSection';
import { ProcessSection } from '@/components/home/ProcessSection';
import { FAQPreview } from '@/components/home/FAQPreview';
import { CTASection } from '@/components/home/CTASection';

export const metadata: Metadata = {
  title: '정책자금 컨설팅 | 중소기업 소상공인 창업자금 전문 상담',
  description: '중소기업·소상공인 정책자금 전문 컨설팅. 기업심사관 출신 경영컨설턴트의 무료 상담.',
  alternates: { canonical: 'https://pola.com' },
};

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <ServiceSection />
      <ProcessSection />
      <FAQPreview />
      <CTASection />
    </>
  );
}
```

---

### 2.2 페이지 라우트 그룹

#### (marketing)/ 그룹 - 마케팅 페이지
```
app/(marketing)/
├── company/
│   └── page.tsx                  # 회사소개
├── process/
│   └── page.tsx                  # 진행과정
├── fund/
│   └── page.tsx                  # 자금상담
├── service/
│   └── page.tsx                  # 전문서비스
├── success/
│   ├── page.tsx                  # 성공사례 목록
│   └── [id]/
│       └── page.tsx              # 개별 사례 상세
├── faq/
│   └── page.tsx                  # 자주묻는질문
├── blog/
│   ├── page.tsx                  # 블로그 목록
│   └── [slug]/
│       └── page.tsx              # 블로그 글 상세
└── contact/
    └── page.tsx                  # 상담신청
```

**라우트 그룹 장점**:
- 공통 레이아웃 적용 가능
- URL에 영향 없음 (예: `/company`, `/fund`)
- 논리적 그룹화

#### (legal)/ 그룹 - 법적 페이지
```
app/(legal)/
├── privacy/
│   └── page.tsx                  # 개인정보 처리방침
├── terms/
│   └── page.tsx                  # 이용약관
└── layout.tsx                    # 법적 페이지 전용 레이아웃
```

---

### 2.3 API Routes

```
app/api/
├── submit-form/
│   └── route.ts                  # POST /api/submit-form
├── send-email/
│   └── route.ts                  # POST /api/send-email
├── backup/
│   └── route.ts                  # GET /api/backup (Cron)
└── health/
    └── route.ts                  # GET /api/health
```

#### API Route 예시
```typescript
// app/api/submit-form/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { createRecord } from '@/lib/airtable';
import { sendEmail } from '@/lib/email';
import { sendTelegram } from '@/lib/telegram';
import { z } from 'zod';

const formSchema = z.object({
  name: z.string().min(2),
  phone: z.string().regex(/^01[0-9]-?[0-9]{3,4}-?[0-9]{4}$/),
  email: z.string().email().optional(),
  company: z.string().optional(),
  amount: z.number().optional(),
  content: z.string().optional(),
});

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const data = formSchema.parse(body);

    // Airtable 저장
    const record = await createRecord(data);

    // 이메일 발송
    await sendEmail({
      to: data.email,
      subject: '[POLA] 신청이 완료되었습니다',
      template: 'confirmation',
      data,
    });

    // Telegram 알림
    await sendTelegram(`🔔 신규 신청: ${data.name} (${data.phone})`);

    return NextResponse.json({
      success: true,
      message: '신청이 완료되었습니다',
      data: { id: record.id },
    });
  } catch (error) {
    return NextResponse.json(
      { success: false, error: error.message },
      { status: 400 }
    );
  }
}
```

---

### 2.4 동적 OG 이미지

```
app/og/
└── route.tsx                     # 동적 Open Graph 이미지
```

```tsx
// app/og/route.tsx
import { ImageResponse } from 'next/og';

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const title = searchParams.get('title') || '정책자금 컨설팅';

  return new ImageResponse(
    (
      <div
        style={{
          display: 'flex',
          fontSize: 60,
          color: 'white',
          background: 'linear-gradient(135deg, #0066CC 0%, #003D7A 100%)',
          width: '100%',
          height: '100%',
          padding: '50px 80px',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <h1>{title}</h1>
      </div>
    ),
    {
      width: 1200,
      height: 630,
    }
  );
}
```

---

## 3. components/ 디렉토리 상세

### 3.1 layout/ - 레이아웃 컴포넌트

```
components/layout/
├── Header.tsx                    # 헤더 (메뉴)
├── Footer.tsx                    # 푸터
├── MobileMenu.tsx                # 모바일 메뉴
├── Breadcrumb.tsx                # 빵가루 네비게이션 (SEO)
└── Container.tsx                 # 반응형 컨테이너
```

#### Header.tsx
```tsx
// components/layout/Header.tsx
'use client';

import Link from 'next/link';
import { useState } from 'react';
import { MobileMenu } from './MobileMenu';

const NAV_ITEMS = [
  { label: '회사소개', href: '/company' },
  { label: '진행과정', href: '/process' },
  { label: '자금상담', href: '/fund' },
  { label: '전문서비스', href: '/service' },
  { label: '성공사례', href: '/success' },
  { label: 'FAQ', href: '/faq' },
  { label: '블로그', href: '/blog' },
];

export function Header() {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  return (
    <header className="sticky top-0 z-50 bg-white shadow-sm">
      <div className="container mx-auto px-4">
        <div className="flex items-center justify-between h-16">
          {/* 로고 */}
          <Link href="/" className="text-2xl font-bold text-primary">
            POLA
          </Link>

          {/* 데스크톱 메뉴 */}
          <nav className="hidden md:flex items-center gap-8">
            {NAV_ITEMS.map((item) => (
              <Link
                key={item.href}
                href={item.href}
                className="text-gray-700 hover:text-primary transition-colors"
              >
                {item.label}
              </Link>
            ))}
          </nav>

          {/* CTA 버튼 */}
          <Link
            href="/contact"
            className="hidden md:inline-block bg-primary text-white px-6 py-2 rounded-lg hover:bg-primary/90"
          >
            무료 상담
          </Link>

          {/* 모바일 메뉴 버튼 */}
          <button
            className="md:hidden"
            onClick={() => setIsMobileMenuOpen(true)}
            aria-label="메뉴 열기"
          >
            <MenuIcon />
          </button>
        </div>
      </div>

      {/* 모바일 메뉴 */}
      <MobileMenu
        isOpen={isMobileMenuOpen}
        onClose={() => setIsMobileMenuOpen(false)}
        items={NAV_ITEMS}
      />
    </header>
  );
}
```

---

### 3.2 home/ - 홈페이지 섹션

```
components/home/
├── HeroSection.tsx               # 히어로 섹션
├── ServiceSection.tsx            # 서비스 소개
├── ProcessSection.tsx            # 진행 과정
├── StatsSection.tsx              # 통계 (선택)
├── FAQPreview.tsx                # FAQ 미리보기
└── CTASection.tsx                # 상담 신청 CTA
```

#### HeroSection.tsx
```tsx
// components/home/HeroSection.tsx
import Link from 'next/link';
import Image from 'next/image';

export function HeroSection() {
  return (
    <section className="relative bg-gradient-to-br from-primary to-primary-dark text-white py-20 md:py-32">
      <div className="container mx-auto px-4">
        <div className="grid md:grid-cols-2 gap-12 items-center">
          {/* 텍스트 */}
          <div>
            <div className="inline-block bg-white/10 text-white px-4 py-2 rounded-full text-sm font-semibold mb-6">
              정책자금 전문 컨설팅
            </div>

            <h1 className="text-4xl md:text-6xl font-bold mb-6 leading-tight">
              중소기업·소상공인
              <br />
              정책자금 맞춤 지원
            </h1>

            <p className="text-xl md:text-2xl text-white/90 mb-8">
              기업심사관 출신 경영컨설턴트의 1:1 무료 상담
            </p>

            <div className="flex flex-col sm:flex-row gap-4">
              <Link
                href="/contact"
                className="inline-block bg-accent text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-accent/90 transition-colors text-center"
              >
                무료 상담 신청
              </Link>
              <Link
                href="/success"
                className="inline-block bg-white/10 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-white/20 transition-colors text-center border border-white/30"
              >
                성공사례 보기
              </Link>
            </div>

            {/* 통계 */}
            <div className="grid grid-cols-3 gap-6 mt-12">
              <div>
                <div className="text-3xl font-bold text-accent">95%</div>
                <div className="text-white/80">승인률</div>
              </div>
              <div>
                <div className="text-3xl font-bold text-accent">2,000+</div>
                <div className="text-white/80">성공사례</div>
              </div>
              <div>
                <div className="text-3xl font-bold text-accent">15년</div>
                <div className="text-white/80">경력</div>
              </div>
            </div>
          </div>

          {/* 이미지 */}
          <div className="relative h-[400px] md:h-[500px]">
            <Image
              src="/images/hero/main.jpg"
              alt="정책자금 컨설팅 POLA"
              fill
              className="object-contain"
              priority
            />
          </div>
        </div>
      </div>
    </section>
  );
}
```

---

### 3.3 success/ - 성공사례 컴포넌트

```
components/success/
├── CaseCard.tsx                  # 사례 카드
├── CaseDetail.tsx                # 사례 상세
├── TestimonialList.tsx           # 후기 목록
└── FilterBar.tsx                 # 필터 바 (업종별)
```

#### CaseCard.tsx
```tsx
// components/success/CaseCard.tsx
import Link from 'next/link';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

interface CaseCardProps {
  id: string;
  industry: string;
  amount: string;
  fundType: string;
  duration: string;
  customerName: string;
  rating: number;
}

export function CaseCard({ id, industry, amount, fundType, duration, customerName, rating }: CaseCardProps) {
  return (
    <Link href={`/success/${id}`}>
      <Card className="hover:shadow-lg transition-shadow cursor-pointer">
        <CardHeader>
          <div className="flex items-center justify-between mb-2">
            <span className="inline-block bg-primary/10 text-primary px-3 py-1 rounded-full text-sm font-semibold">
              {industry}
            </span>
            <span className="text-accent font-bold text-lg">{amount}</span>
          </div>
          <CardTitle className="text-xl">{fundType} 승인 사례</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="space-y-2 text-sm text-gray-600">
            <div className="flex items-center gap-2">
              <ClockIcon className="w-4 h-4" />
              <span>소요 기간: {duration}</span>
            </div>
            <div className="flex items-center gap-2">
              <UserIcon className="w-4 h-4" />
              <span>{customerName}</span>
            </div>
            <div className="flex items-center gap-1">
              {Array.from({ length: 5 }).map((_, i) => (
                <StarIcon
                  key={i}
                  className={`w-4 h-4 ${i < rating ? 'text-yellow-400 fill-yellow-400' : 'text-gray-300'}`}
                />
              ))}
            </div>
          </div>
        </CardContent>
      </Card>
    </Link>
  );
}
```

---

### 3.4 blog/ - 블로그 컴포넌트

```
components/blog/
├── PostCard.tsx                  # 블로그 카드
├── PostContent.tsx               # 글 본문 (MDX)
├── RelatedPosts.tsx              # 관련 글
├── TableOfContents.tsx           # 목차
└── ShareButtons.tsx              # 공유 버튼
```

---

### 3.5 faq/ - FAQ 컴포넌트

```
components/faq/
├── FAQAccordion.tsx              # FAQ 아코디언
└── FAQSearch.tsx                 # FAQ 검색
```

#### FAQAccordion.tsx
```tsx
// components/faq/FAQAccordion.tsx
'use client';

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/ui/accordion';

interface FAQ {
  id: string;
  question: string;
  answer: string;
  category: string;
}

interface FAQAccordionProps {
  faqs: FAQ[];
}

export function FAQAccordion({ faqs }: FAQAccordionProps) {
  return (
    <Accordion type="single" collapsible className="w-full">
      {faqs.map((faq) => (
        <AccordionItem key={faq.id} value={faq.id}>
          <AccordionTrigger className="text-left">
            <span className="font-semibold">{faq.question}</span>
          </AccordionTrigger>
          <AccordionContent>
            <div className="text-gray-600 leading-relaxed">
              {faq.answer}
            </div>
          </AccordionContent>
        </AccordionItem>
      ))}
    </Accordion>
  );
}
```

---

### 3.6 forms/ - 폼 컴포넌트

```
components/forms/
├── ContactForm.tsx               # 상담 신청 폼
├── NewsletterForm.tsx            # 뉴스레터 구독
└── FormField.tsx                 # 공통 폼 필드
```

#### ContactForm.tsx
```tsx
// components/forms/ContactForm.tsx
'use client';

import { useState } from 'react';
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Textarea } from '@/components/ui/textarea';
import { useToast } from '@/hooks/use-toast';

const formSchema = z.object({
  name: z.string().min(2, '이름은 2자 이상 입력해주세요'),
  phone: z.string().regex(/^01[0-9]-?[0-9]{3,4}-?[0-9]{4}$/, '올바른 전화번호를 입력해주세요'),
  email: z.string().email('올바른 이메일을 입력해주세요').optional().or(z.literal('')),
  company: z.string().optional(),
  amount: z.string().optional(),
  content: z.string().max(1000, '최대 1,000자까지 입력 가능').optional(),
});

type FormData = z.infer<typeof formSchema>;

export function ContactForm() {
  const [isSubmitting, setIsSubmitting] = useState(false);
  const { toast } = useToast();

  const {
    register,
    handleSubmit,
    formState: { errors },
    reset,
  } = useForm<FormData>({
    resolver: zodResolver(formSchema),
  });

  const onSubmit = async (data: FormData) => {
    setIsSubmitting(true);

    try {
      const response = await fetch('/api/submit-form', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      const result = await response.json();

      if (result.success) {
        toast({
          title: '신청이 완료되었습니다',
          description: '담당 컨설턴트가 곧 연락드리겠습니다.',
        });
        reset();
      } else {
        throw new Error(result.error);
      }
    } catch (error) {
      toast({
        title: '신청에 실패했습니다',
        description: error.message,
        variant: 'destructive',
      });
    } finally {
      setIsSubmitting(false);
    }
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-6">
      <div>
        <label htmlFor="name" className="block text-sm font-medium mb-2">
          이름 <span className="text-red-500">*</span>
        </label>
        <Input
          id="name"
          {...register('name')}
          placeholder="홍길동"
          aria-invalid={!!errors.name}
          aria-describedby={errors.name ? 'name-error' : undefined}
        />
        {errors.name && (
          <p id="name-error" className="mt-1 text-sm text-red-500" role="alert">
            {errors.name.message}
          </p>
        )}
      </div>

      <div>
        <label htmlFor="phone" className="block text-sm font-medium mb-2">
          연락처 <span className="text-red-500">*</span>
        </label>
        <Input
          id="phone"
          type="tel"
          {...register('phone')}
          placeholder="010-1234-5678"
          aria-invalid={!!errors.phone}
        />
        {errors.phone && (
          <p className="mt-1 text-sm text-red-500" role="alert">
            {errors.phone.message}
          </p>
        )}
      </div>

      <div>
        <label htmlFor="email" className="block text-sm font-medium mb-2">
          이메일
        </label>
        <Input
          id="email"
          type="email"
          {...register('email')}
          placeholder="example@email.com"
        />
      </div>

      <div>
        <label htmlFor="company" className="block text-sm font-medium mb-2">
          회사명
        </label>
        <Input
          id="company"
          {...register('company')}
          placeholder="(주)회사명"
        />
      </div>

      <div>
        <label htmlFor="amount" className="block text-sm font-medium mb-2">
          희망 금액
        </label>
        <Input
          id="amount"
          {...register('amount')}
          placeholder="예: 5천만원"
        />
      </div>

      <div>
        <label htmlFor="content" className="block text-sm font-medium mb-2">
          상담 내용
        </label>
        <Textarea
          id="content"
          {...register('content')}
          placeholder="자세한 상담 내용을 입력해주세요"
          rows={4}
        />
        {errors.content && (
          <p className="mt-1 text-sm text-red-500" role="alert">
            {errors.content.message}
          </p>
        )}
      </div>

      <Button
        type="submit"
        disabled={isSubmitting}
        className="w-full"
        size="lg"
      >
        {isSubmitting ? '제출 중...' : '무료 상담 신청'}
      </Button>
    </form>
  );
}
```

---

### 3.7 seo/ - SEO 컴포넌트

```
components/seo/
├── StructuredData.tsx            # JSON-LD 스키마
└── MetaTags.tsx                  # 공통 메타 태그
```

#### StructuredData.tsx
```tsx
// components/seo/StructuredData.tsx
interface StructuredDataProps {
  type: 'organization' | 'service' | 'faq' | 'article';
  data?: any;
}

export function StructuredData({ type, data }: StructuredDataProps) {
  const schemas = {
    organization: {
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'POLA 정책자금 컨설팅',
      url: 'https://pola.com',
      logo: 'https://pola.com/logo.png',
      // ...
    },
    // ... 기타 스키마
  };

  const schema = schemas[type];

  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}
    />
  );
}
```

---

### 3.8 ui/ - shadcn/ui 컴포넌트

```
components/ui/
├── button.tsx                    # 버튼
├── input.tsx                     # 입력
├── card.tsx                      # 카드
├── accordion.tsx                 # 아코디언 (FAQ)
├── dialog.tsx                    # 모달
├── toast.tsx                     # 토스트
└── ... (기타 shadcn/ui)
```

---

## 4. lib/ 디렉토리 상세

```
lib/
├── airtable.ts                   # Airtable SDK 래퍼
├── email.ts                      # Resend SDK 래퍼
├── telegram.ts                   # Telegram Bot API
├── seo-analytics.ts              # SEO 분석
├── keywords.ts                   # 키워드 관리
├── utils.ts                      # 공통 유틸
└── constants.ts                  # 상수 정의
```

### airtable.ts
```typescript
// lib/airtable.ts
import Airtable from 'airtable';

const base = new Airtable({ apiKey: process.env.AIRTABLE_TOKEN }).base(
  process.env.AIRTABLE_BASE_ID!
);

export async function createRecord(data: any) {
  const record = await base('고객정보').create({
    이름: data.name,
    연락처: data.phone,
    이메일: data.email,
    회사명: data.company,
    희망금액: data.amount,
    상담내용: data.content,
    신청경로: 'website',
    처리상태: '대기중',
  });

  return {
    id: record.id,
    createdAt: record.get('신청일시'),
  };
}

export async function getRecords() {
  const records = await base('고객정보').select().all();
  return records.map((r) => ({
    id: r.id,
    name: r.get('이름'),
    phone: r.get('연락처'),
    status: r.get('처리상태'),
  }));
}
```

### constants.ts
```typescript
// lib/constants.ts
export const SITE_CONFIG = {
  name: 'POLA',
  fullName: 'POLA 정책자금 컨설팅',
  url: 'https://pola.com',
  description: '중소기업·소상공인 정책자금 전문 컨설팅',
  phone: '02-XXXX-XXXX',
  email: 'info@pola.com',
  address: '서울특별시 강남구 XXX로 123',
};

export const POLICY_FUND_INFO = {
  interestRate: '2.99% ~ 3.39%',
  limit: {
    startup: '1억원',
    working: '5억원',
    facility: '60억원',
  },
  updatedAt: '2025-11-24',
};

export const NAV_ITEMS = [
  { label: '회사소개', href: '/company' },
  { label: '진행과정', href: '/process' },
  { label: '자금상담', href: '/fund' },
  { label: '전문서비스', href: '/service' },
  { label: '성공사례', href: '/success' },
  { label: 'FAQ', href: '/faq' },
  { label: '블로그', href: '/blog' },
];
```

---

## 5. data/ 디렉토리 상세

```
data/
├── success-cases.json            # 성공사례 데이터
├── faq-data.json                 # FAQ 데이터
└── blog-posts/                   # 블로그 글 (MDX)
    ├── 2025-policy-fund-guide.mdx
    ├── startup-funding-tips.mdx
    └── business-plan-guide.mdx
```

### success-cases.json
```json
// data/success-cases.json
[
  {
    "id": "case-001",
    "industry": "제조업",
    "fundType": "시설자금",
    "amount": "10억원",
    "duration": "3주",
    "customerName": "김OO 대표",
    "rating": 5,
    "description": "제조업 설비 투자를 위한 시설자금 10억원 승인 사례입니다...",
    "tags": ["제조업", "시설자금", "대출"],
    "createdAt": "2025-01-15"
  },
  {
    "id": "case-002",
    "industry": "음식점",
    "fundType": "창업자금",
    "amount": "5천만원",
    "duration": "2주",
    "customerName": "이OO 대표",
    "rating": 5,
    "description": "음식점 창업을 위한 청년창업자금 5천만원 승인...",
    "tags": ["음식점", "창업자금", "청년"],
    "createdAt": "2025-01-10"
  }
]
```

### faq-data.json
```json
// data/faq-data.json
[
  {
    "id": "faq-001",
    "category": "신청 조건",
    "question": "정책자금 신청 자격이 어떻게 되나요?",
    "answer": "중소기업기본법상 중소기업 또는 소상공인으로 사업자등록이 되어 있으면 신청 가능합니다. 업력, 신용등급, 담보 유무에 따라 한도가 달라질 수 있습니다.",
    "order": 1
  },
  {
    "id": "faq-002",
    "category": "금리 및 한도",
    "question": "정책자금 금리는 얼마인가요?",
    "answer": "2025년 기준 연 2.99%~3.39% 수준입니다. 기준금리 변동에 따라 분기별로 조정됩니다.",
    "order": 2
  }
]
```

---

## 6. public/ 디렉토리 상세

```
public/
├── images/
│   ├── hero/
│   │   ├── main.jpg              # 히어로 메인 이미지
│   │   └── main.webp             # WebP 버전
│   ├── services/
│   │   ├── consulting.jpg
│   │   ├── planning.jpg
│   │   └── analysis.jpg
│   ├── cases/
│   │   ├── case-001.jpg
│   │   └── case-002.jpg
│   ├── blog/
│   │   └── thumbnails/
│   └── og/
│       ├── og-home.jpg           # 홈 OG 이미지
│       └── og-default.jpg        # 기본 OG 이미지
├── fonts/
│   └── PretendardVariable.woff2  # Pretendard 폰트
├── favicon.ico                   # 파비콘
├── logo.png                      # 로고
└── robots.txt                    # (자동 생성됨)
```

---

## 7. 설정 파일

### next.config.js
```javascript
// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'pola.com',
      },
    ],
  },
  experimental: {
    optimizeCss: true,
  },
};

module.exports = nextConfig;
```

### tailwind.config.ts
```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: [
    './app/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#0066CC',
          dark: '#003D7A',
          light: '#E6F2FF',
        },
        accent: '#F59E0B',
      },
      fontFamily: {
        pretendard: ['var(--font-pretendard)', 'sans-serif'],
      },
    },
  },
  plugins: [],
};

export default config;
```

### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

---

## 8. 네이밍 컨벤션

### 파일 네이밍

| 타입 | 규칙 | 예시 |
|------|------|------|
| **React 컴포넌트** | PascalCase | `HeroSection.tsx` |
| **페이지** | lowercase | `page.tsx`, `layout.tsx` |
| **API Routes** | lowercase | `route.ts` |
| **유틸리티** | kebab-case | `seo-analytics.ts` |
| **타입 정의** | PascalCase | `Airtable.ts` |
| **상수** | lowercase | `constants.ts` |
| **훅** | kebab-case | `use-form-submission.ts` |
| **데이터** | kebab-case | `success-cases.json` |

### 변수 네이밍

```typescript
// 컴포넌트 - PascalCase
export function HeroSection() {}

// 상수 - UPPER_SNAKE_CASE
const MAX_FILE_SIZE = 5 * 1024 * 1024;

// 변수/함수 - camelCase
const formData = {};
function handleSubmit() {}

// 타입/인터페이스 - PascalCase
interface FormData {}
type SuccessCase = {};

// Private 변수 - _camelCase (선택)
const _internalState = {};
```

---

## 9. Import 경로 규칙

### 절대 경로 사용 (@/ 별칭)

```typescript
// ✅ 올바른 방법
import { Button } from '@/components/ui/button';
import { createRecord } from '@/lib/airtable';
import { SITE_CONFIG } from '@/lib/constants';

// ❌ 잘못된 방법
import { Button } from '../../../components/ui/button';
import { createRecord } from '../../lib/airtable';
```

### Import 순서

```typescript
// 1. React & Next.js
import { useState } from 'react';
import Link from 'next/link';
import Image from 'next/image';

// 2. 외부 라이브러리
import { z } from 'zod';
import { useForm } from 'react-hook-form';

// 3. 내부 컴포넌트
import { Button } from '@/components/ui/button';
import { Card } from '@/components/ui/card';

// 4. 내부 라이브러리
import { createRecord } from '@/lib/airtable';
import { SITE_CONFIG } from '@/lib/constants';

// 5. 타입
import type { Metadata } from 'next';
import type { FormData } from '@/types';

// 6. 스타일
import './styles.css';
```

---

## 10. 환경변수 관리

### .env.local (Git 제외)
```bash
# Airtable
AIRTABLE_TOKEN=patXXXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXXXX

# Resend (Email)
RESEND_API_KEY=re_XXXXXXXXXXXXXXXXXX

# Telegram
TELEGRAM_BOT_TOKEN=XXXXXXXXX:XXXXXXXXXXXXXXXXXXXXXXXXXXX
TELEGRAM_CHAT_ID=-XXXXXXXXXXXXX

# Site URL
NEXT_PUBLIC_SITE_URL=https://pola.com

# Google Search Console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=XXXXXXXXXXXXXXXXXX
```

### .env.example (Git 포함)
```bash
# Airtable
AIRTABLE_TOKEN=your_token_here
AIRTABLE_BASE_ID=your_base_id_here

# Resend (Email)
RESEND_API_KEY=your_api_key_here

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Site URL
NEXT_PUBLIC_SITE_URL=https://pola.com

# Google Search Console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_verification_code_here
```

---

## 11. Git 제외 파일 (.gitignore)

```
# .gitignore
# Dependencies
node_modules/

# Next.js
.next/
out/
build/

# 환경변수
.env.local
.env.*.local

# 로그
*.log
npm-debug.log*

# 에디터
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Backups
backups/

# 테스트
coverage/
.nyc_output/

# TypeScript
*.tsbuildinfo
```

---

## 12. 프로젝트 초기 설정 명령어

```bash
# 1. Next.js 프로젝트 생성
npx create-next-app@latest pola-homepage --typescript --tailwind --app --src-dir=false

# 2. 디렉토리 이동
cd pola-homepage

# 3. shadcn/ui 초기화
npx shadcn@latest init

# 4. 필수 컴포넌트 설치
npx shadcn@latest add button input card accordion dialog toast

# 5. 추가 패키지 설치
npm install airtable resend react-hook-form zod @hookform/resolvers

# 6. 개발 서버 실행
npm run dev
```

---

**문서 버전**: 1.0.0
**작성일**: 2025-11-24
**작성자**: Claude (AI Assistant)
